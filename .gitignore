__pycache__/
*.pyc
.venv/
venv/
config.toml
*.db
*.db-wal
*.db-shm
/lance/
.cache/
# Wikipedia ZIMs live in their own folder, never in the document `sources` crawl.
/wikipedia/
*.zim
*.usearch
*.ann.ids.json

# Third-party bulk datasets (ConceptNet, CauseNet, GLUCOSE, ATOMIC, …) and any
# personal documents are downloaded separately — see external/README.md.
external/*
!external/README.md

# In-tree everything (see env.sh): indexes/kb/caches/tmp, the reranker GGUF,
# and the bootstrapped uv binary.
/var/
/models/
/bin/

# Editor / OS noise
.DS_Store
._*
*.swp
.idea/
.vscode/
